# tuples are also like list but hteu are immutable
example=("kamlesh",4,"rajuuu")
# no append no pop no remove
# we can store in other list but cannnot change  iter
# we use when we have not to change our data like weeks monts 
example[1]="kamlesh"
print(example)
# why error because it is touple
